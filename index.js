// Code your solutions in this file
let stringNames = ["Guadalupe", "Ollie", "Aki"];
function writeCards(stringNames) {
    return [
        `Thank you, ${stringNames[0]}, for the wonderful surprise gift!`,
        `Thank you, ${stringNames[1]}, for the wonderful surprise gift!`,
        `Thank you, ${stringNames[2]}, for the wonderful surprise gift!`,
    ];
};

function countDown() {
    for (let spy = 10; spy >= 0; spy--) {
    console.log(spy)
    };
};

for (let spy = 4; spy >= 0; spy--) {
console.log(spy)
}
